/**
 * Shared Hooks Index
 *
 * Re-exports all shared hooks for convenient imports.
 * Usage: import { useDebounce, useToast } from '@/hooks';
 */

export { useDebounce, useDebouncedCallback } from './useDebounce';
export { useToast } from './useToast';
