# Environment Variables Template
# Copy this file to .env.local and fill in actual values

# ===========================================
# Supabase Configuration (Story 1.6)
# ===========================================
# Get these values from your Supabase project dashboard:
# https://supabase.com/dashboard/project/YOUR_PROJECT_ID/settings/api

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ===========================================
# Fangraphs API Configuration (Story 4.4)
# ===========================================
# Fangraphs projections API is public - no API key required
# Endpoint: https://www.fangraphs.com/api/projections
#
# Query parameters:
#   type: steamer, thebat, thebatx, atc, zips
#   stats: bat (hitters), pit (pitchers)
#   pos: all
#   team: 0 (all teams)
#   players: 0 (all players)
#   lg: all
#
# Example: ?type=steamer&stats=bat&pos=all&team=0&players=0&lg=all

# ===========================================
# Couch Managers Integration (Story 9.1)
# ===========================================
# Couch Managers does not offer a public API.
# Draft data is scraped from public auction pages:
# https://www.couchmanagers.com/auctions/?auction_id={auctionId}
#
# No API keys required - we parse the HTML/JavaScript from public pages.
# The Edge Function handles scraping server-side to avoid CORS issues.

# ===========================================
# Notes
# ===========================================
# - All VITE_ prefixed variables are exposed to the client
# - Never commit .env.local to version control
# - Supabase credentials will be configured in Story 1.6
